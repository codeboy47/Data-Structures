/// partition of set into two subsets such that the sum of difference of 2 subsets is minimum
#include<iostream>
#include<unordered_map>
using namespace std;
void verticalPrint(BinaryTreeNode*root,int col,unordered_map<int,vector<int> > &map){
    if(root==NULL) return;
    map[col].push_back(root->data); push value in array

}
int main(){
    BinaryTreeNode*root=NULL;
    unordered_map<int,vector<int> > mymap; /// takes key(int) and values(which is stored in array)
    root = take
    verticalPrint(root,0,mymap);
}
 //8147038  + 45 + 25 -> 8147108
 //9142244 -> 9142356
